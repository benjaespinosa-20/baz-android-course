package com.example.criptobenjaespi.data.local

import androidx.room.Dao
import androidx.room.Insert
import androidx.room.OnConflictStrategy
import androidx.room.Query
import com.example.criptobenjaespi.data.model.PayloadEntity

/*
@Dao
interface CriptoDao {
    @Query("SELECT * FROM payloadentity")
    suspend fun getCriptosList(): List<PayloadEntity>

    @Insert(onConflict = OnConflictStrategy.REPLACE)
    suspend fun saveCriptos(cripto: PayloadEntity)
}*/
